{
    "endpoints": [
        {
            "url": "/logger/*",
            "method": "POST",
            "response": {
                "type": "custom",
                "file": "../plugins/endpoint_logger.so"
            }
        }
    ]
}